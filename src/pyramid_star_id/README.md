# pyramid_star_id

Small package that implements a pyramid-based star identification pipeline.
See examples/demo.py for usage.

Install: copy the folder into your PYTHONPATH or run `pip install .` in the folder.
To run demo: python -m pyramid_star_id.examples.demo